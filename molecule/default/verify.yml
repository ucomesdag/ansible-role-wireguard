---
- name: Verify
  hosts: all
  become: false
  gather_facts: false

  tasks:
    - name: test connection
      ping:

    - name: try the package module
      package:
        name: httpd
        state: present
